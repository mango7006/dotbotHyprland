monitor = eDP-1, 1920x1080@60, 0x0, 1
# Screen mirror any connected display
monitor = , preferred, auto, 1, mirror, eDP-1

# Set programs that you use
$terminal = kitty
$menu = wofi --show drun --allow-images
$browser = firefox
$wallpaper = ~/.config/hypr/wallpapers/orange-mountain-church.png

exec-once = swww-daemon
exec-once = swww img $wallpaper --transition-type none
exec-once = waybar
# exec-once = swaync
exec-once = clipse -listen
exec-once = hypridle
exec-once = syshud -p top

exec-once = rm ~/ly-session.log

exec-once = $terminal --class terminalfloatsmall -e ~/.config/scripts/wireguard-master.sh toggle RPI-Home

exec-once = hyprctl setcursor Adwaita 14
exec-once = gsettings set org.gnome.desktop.interface color-scheme 'prefer-dark'
exec-once = gsettings set org.gnome.desktop.interface cursor-theme Adwaita

env = XCURSOR_THEME,Adwaita
env = XCURSOR_SIZE,14
env = HYPRCURSOR_SIZE,14
env = HYPRCURSOR_THEME,Adwaita
env = TERM,$terminal

general {
  gaps_in = 6
  gaps_out = 12
  border_size = 4
  col.active_border = rgb(d65d0e)
  col.inactive_border = rgb(3c3836)
  layout = dwindle
  allow_tearing = false
}

decoration {
  active_opacity = 1
  inactive_opacity = 1
  rounding = 0
  blur {
    enabled = true
    size = 6
    passes = 3
    contrast = 2.0
    vibrancy_darkness = 1.0
    brightness = 1.3
    vibrancy = 1
  }
  shadow {
    enabled = no
  }
}

ecosystem {
  no_update_news = true
}

animations {
  enabled = yes

  bezier = easeOutQuint,0.23,1,0.32,1
  bezier = easeInOutCubic,0.65,0.05,0.36,1
  bezier = linear,0,0,1,1
  bezier = almostLinear,0.5,0.5,0.75,1.0
  bezier = quick,0.15,0,0.1,1

  animation = global, 1, 10, default
  animation = border, 1, 5.39, easeOutQuint
  animation = windows, 1, 4.79, easeOutQuint
  animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
  animation = windowsOut, 1, 1.49, linear, popin 87%
  animation = fadeIn, 1, 1.73, almostLinear
  animation = fadeOut, 1, 1.46, almostLinear
  animation = fade, 1, 3.03, quick
  animation = layers, 1, 3.81, easeOutQuint
  animation = layersIn, 1, 4, easeOutQuint, fade
  animation = layersOut, 1, 1.5, linear, fade
  animation = fadeLayersIn, 1, 1.79, almostLinear
  animation = fadeLayersOut, 1, 1.39, almostLinear
  animation = workspaces, 1, 1.94, almostLinear, fade
  animation = workspacesIn, 1, 1.21, almostLinear, fade
  animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ignore maximize requests from apps
# windowrule = suppress_event maximize, match:class .*

# Fix some dragging issues with XWayland
windowrule = no_focus on, match:class ^$, match:title ^$, match:xwayland 1, match:float 1, match:fullscreen 0, match:pin 0

# I don't remember what does does but it seems important
windowrule = rounding 0, match:float 0, match:workspace f[1]TSInstall hyprlang

# Disable screensharing of passwords
windowrule = no_screen_share on, match:class Bitwarden

# Blur rules
layerrule = blur on, match:namespace waybar
layerrule = blur on, no_anim on, match:namespace wofi

# Force these apps to float
windowrule = float on, size 1000 600, center on, match:class terminalfloat
windowrule = float on, size 430 100, center on, match:class terminalfloatsmall
windowrule = float on, size 800 600, center on, match:class waypaper
windowrule = float on, size 1000 600, center on, match:class wiremix
windowrule = float on, size 1000 600, center on, match:class nwg-look
windowrule = float on, size 1000 600, center on, match:class bluetui
windowrule = float on, size 1200 700, center on, match:class btop
windowrule = float on, size 1000 600, center on, match:class impala
windowrule = float on, size 750 750, center on, match:class teletekst

dwindle {
  pseudotile = true
  preserve_split = true
}

misc {
  force_default_wallpaper = 0
  vfr = true
  # enable_swallow = true
  # swallow_regex = ^($terminal)$
  disable_hyprland_logo = true
  font_family = Mononoki Nerd Font
  splash_font_family = Mononoki Nerd Font
}

input {
  kb_layout = us,ru,us
  kb_variant = ,phonetic,intl
  follow_mouse = 1
  numlock_by_default = true
  sensitivity = 0

  touchpad {
    natural_scroll = false
    tap-to-click = false
    disable_while_typing = false # disable_while_typing sucks ass
  }
}

bind = SUPER, Q, exec, $terminal
bind = SUPER, C, killactive,
# bind = SUPER, M, exit, # Do not use this, will kill all applications
bind = SUPER, V, togglefloating,
bind = SUPER SHIFT, F, fullscreen
bind = SUPER, A, exec, $menu
bind = SUPER, F, exec, $browser 
bind = SUPER, P, exec, $browser --private-window
bind = SUPER, L, exec, hyprlock
bind = SUPER, Z, exec, ~/.config/waybar/scripts/kbd-hypr.sh next

# The actions menu, a list of the most used scripts/actions on my system
bind = SUPER, X, exec, ~/.config/scripts/shutdown-menu.sh
bind = SUPER, S, exec, ~/.config/scripts/actions-menu.sh

# bind = SUPER, H, exec, swaync-client -t -sw
# bind = SUPER, J, exec, swaync-client -C
bind = SUPER, D, exec, $terminal --class terminalfloat -e clipse

# Move focus with mainMod + arrow keys
bind = SUPER, left, movefocus, l
bind = SUPER, right, movefocus, r
bind = SUPER, up, movefocus, u
bind = SUPER, down, movefocus, d

# Screenshot selected area
bindlr = , print, exec, hyprshot -m region -o ~/Screenshots

# Resize windows with CRTL + arrow keys
bindel = CTRL, right, resizeactive, 20 0
bindel = CTRL, left, resizeactive, -20 0
bindel = CTRL, up, resizeactive, 0 -20
bindel = CTRL, down, resizeactive, 0 20

# Move windows with SHIFT + arrow keys
bind = SHIFT, left, movewindow, l
bind = SHIFT, right, movewindow, r
bind = SHIFT, up, movewindow, u
bind = SHIFT, down, movewindow, d

# Switch workspaces with mainMod + [0-4] (limit 4 otherwise I get distracted)
bind = SUPER, 1, workspace, 1
bind = SUPER, 2, workspace, 2
bind = SUPER, 3, workspace, 3
bind = SUPER, 4, workspace, 4

# Move active window to a workspace with mainMod + SHIFT + [0-4] (limit 4 otherwise I get distracted)
bind = SUPER SHIFT, 1, movetoworkspace, 1
bind = SUPER SHIFT, 2, movetoworkspace, 2
bind = SUPER SHIFT, 3, movetoworkspace, 3
bind = SUPER SHIFT, 4, movetoworkspace, 4

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = SUPER, mouse:272, movewindow
bindm = SUPER, mouse:273, resizewindow

# Laptop multimedia keys for volume
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%+ -l 1.3
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 2%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle

# Laptop multimedia keys for LCD brightness
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 5%- --min-value=1

# Lock lid switch - Optional, laptop only
bindl=,switch:off:Lid Switch, exec, hyprlock --immediate
bindl=,switch:on:Lid Switch, exec, hyprlock --immediate

# Multimedia keys for playback control - Requires playerctl and buttons, I don't have buttons :(
# bindl = , XF86AudioNext, exec, playerctl next
# bindl = , XF86AudioPause, exec, playerctl play-pause
# bindl = , XF86AudioPlay, exec, playerctl play-pause
# bindl = , XF86AudioPrev, exec, playerctl previous

# Remove the next 3 binds if you do have buttons
bindl = SUPER, U, exec, playerctl previous
bindl = SUPER, I, exec, playerctl play-pause
bindl = SUPER, O, exec, playerctl next
